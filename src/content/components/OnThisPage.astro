---
const { headings } = Astro.props;
---

<aside class="hidden lg:block w-56 pl-8">
  <div class="sticky w-full">
    <h4 class="on-this-page leading-none">En esta página</h4>
    <ol class="space-y-2">
      {
        headings.map((heading, i) => (
          <li>
            <a
              href={`#${heading.slug}`}
              class="block text-secondary-600 hover:text-secondary-400 text-small"
            >
              {heading.text}
            </a>
          </li>
        ))
      }
    </ol>
  </div>
</aside>
